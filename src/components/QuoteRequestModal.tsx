import React from 'react';
export const QuoteRequestModal = ({ isOpen, onClose }: any) => isOpen ? <div onClick={onClose} className="fixed inset-0 bg-black/50 p-4 text-white">Quote Request Modal. Click to close.</div> : null;