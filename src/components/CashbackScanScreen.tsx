// src/components/CashbackScanScreen.tsx
import React from "react";

const CashbackScanScreen: React.FC = () => {
  return (
    <div style={{ padding: 16 }}>
      <h1>Cashback Scan Screen</h1>
      <p>
        Tela placeholder para o fluxo de leitura do QR do cliente / cashback.
        Depois você conecta com o fluxo real de validação.
      </p>
    </div>
  );
};

export default CashbackScanScreen;
